#include <iostream>

int main()
{
	char ch;
	int num;
	//a
	ch != 'q' && ch != 'k';
	//b
	!(num >= 15 && num <= 26);
	//c
	num % 2 == 0 && num > 30;
	//d
	num % 5 == 0 || num % 8 == 0;
	//e
	float peso, altura;
	!(peso < 50) && altura > 1.60;
}